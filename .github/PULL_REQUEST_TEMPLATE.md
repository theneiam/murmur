## What and why

<!-- One change per PR. Say what changes and why; link the issue if there is one. -->

## Checklist

- [ ] Tests added or updated (pipeline behaviour → `DictationSessionTests`; pure logic → its own test file)
- [ ] `swiftformat --lint .` passes and the build has no new warnings
- [ ] `CHANGELOG.md` *Unreleased* updated if a user would notice
- [ ] `docs/ARCHITECTURE.md` / `CLAUDE.md` updated if responsibilities moved between modules
- [ ] Manually verified in the running app (which apps, which microphone)
